<%= simple_form_for @designer, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :photo %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                designers_path, :class => 'btn' %>
  </div>
<% end %>
