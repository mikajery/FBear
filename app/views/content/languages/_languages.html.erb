<table class="b-menu">
	<tr>
		<td>
			<div>
				<a href="<%= cart_path %>" class="b-cart-link">Корзина</a>
			</div>
		</td>

		<td>
			<div class="b-dropdown">
				<div class="current">
					<%= @language.native %>
				</div>

				<div class="dropdown">
					<div class="wrap">
						<div class="inner">
							<ul>
								<% languages.select{|l| l != @language}.each do |item| %>
									<%= render 'content/languages/item', :item => item %>
								<% end %>
							</ul>
						</div>
					</div>
				</div>
			</div>
		</td>
	</tr>
</table>