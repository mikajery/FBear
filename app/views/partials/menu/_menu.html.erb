<table class="b-menu">
	<tr>
    <% if menu('header') %>
  		<% menu('header').items.each do |item| %>
  			<%= render 'partials/menu/item', :item => item %>
  		<% end %>	
    <% end %>
	</tr>
</table>