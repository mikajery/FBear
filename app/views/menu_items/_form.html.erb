<%= language_prompt %>

<%= simple_form_for @menu_item, :html => { class: 'form-horizontal' }, url: menu_menu_item_path do |f| %>
  <%= f.input :name %>

  <% Globalize.with_locale(@locale.slug) do %>    
    <%= f.input :title %>
  <% end %>

  <%= f.input :url %>

  <%= f.association :page, collection: Page.with_translations('ru'), prompt: 'Выберите страницу' %>

  <%= language_input f %>

  <div class="form-actions">
    <%= f.button :submit, save_or_update(@menu_item), :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                menu_menu_items_path, :class => 'btn' %>
  </div>
<% end %>
