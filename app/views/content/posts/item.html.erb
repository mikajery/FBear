<div class="container">
  <div class="blog">
    <h1>
      <%= @post.title %>
    </h1>

    <div class="blog-info">
      <%= @post.content.html_safe if @post.content %>
    </div>
  </div>

  <% unless @post.comments.empty? %>
    <%= render 'comments', post: @post %>
  <% end %>
</div>